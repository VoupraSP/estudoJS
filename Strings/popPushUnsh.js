let names = []
names.push('Rayam') //Coloca ao final do array
names.push('Náthaly')
names.push('Morgana')
console.log(names, names.length)

names.unshift('lux')//Coloca ao inicio do array
console.log(names, names.length)

names.pop() //Remove o último
console.log(names, names.length)

names.shift() //Remove o primeiro elemento
console.log(names, names.length)